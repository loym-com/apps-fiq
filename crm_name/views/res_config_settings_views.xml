<?xml version="1.0" encoding="utf-8" ?>
<odoo>
    <data>
        <record id="res_config_settings_view_form" model="ir.ui.view">
            <field name="name">res.config.settings.view.form.inherit</field>
            <field name="model">res.config.settings</field>
            <field name="inherit_id" ref="crm.res_config_settings_view_form" />
            <field name="arch" type="xml">
                <xpath expr="//app[@name='crm']/div[@data-string='Display']/div" position="inside">
                    <div class="col-12 col-lg-6 o_setting_box">
                        <label
                            for="crm_lead_name_expression"
                            string="Stored Lead Name Pattern"
                        />
                        <field
                            name="crm_lead_name_expression"
                            class="w-100"
                            placeholder="{r.partner_id.short_name if r.partner_id.short_name else r.partner_id.name}"
                        />
                    </div>
                    <div class="col-12 col-lg-6 o_setting_box">
                        <label
                            for="crm_lead_name_expression_triggers"
                            string="Update Lead Name when these fields change"
                        />
                        <field
                            name="crm_lead_name_expression_triggers"
                            class="w-100"
                            placeholder="partner_id, partner_id.name, partner_id.short_name"
                        />
                    </div>
                </xpath>
            </field>
        </record>
    </data>
</odoo>
